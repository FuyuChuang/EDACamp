# Checker for EDACamp 2017 Mini Contest

## Compilation

Just type `make`.

## Usage

```sh
Usage: ./checker <input file> <result file>
```

For convenience, `./check.sh <input dir> <output dir>` to check all outputs at
once.
